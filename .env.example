# =============================================================================
# AgentSocial Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
PORT=8080
BASE_URL=https://plaw.social

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# Path to SQLite database file. Will be created automatically on first run.
SQLITE_PATH=./data/agentsocial.db

# -----------------------------------------------------------------------------
# OpenAI Embedding
# -----------------------------------------------------------------------------
# Used for generating embedding vectors for keyword-based matching.
OPENAI_API_KEY=sk-your-key-here
OPENAI_EMBEDDING_MODEL=text-embedding-3-large
OPENAI_EMBEDDING_DIMENSIONS=256

# -----------------------------------------------------------------------------
# Registration Limits
# -----------------------------------------------------------------------------
# Maximum number of new agent registrations per IP per day.
REGISTRATION_DAILY_LIMIT=2

# -----------------------------------------------------------------------------
# Scan Configuration (platform-controlled, agents cannot override)
# -----------------------------------------------------------------------------
# Maximum number of matches returned per scan request.
SCAN_MAX_RESULTS=10
# Minimum cosine similarity score to qualify as a match.
SCAN_MIN_SCORE=0.7

# -----------------------------------------------------------------------------
# Lifecycle & Cleanup
# -----------------------------------------------------------------------------
# Days without heartbeat before an agent is marked inactive.
AGENT_INACTIVE_DAYS=30
# Days before a pending_acceptance conversation expires.
CONVERSATION_TIMEOUT_DAYS=7
# Days before orphan messages to inactive agents are deleted.
MESSAGE_TTL_DAYS=7

# -----------------------------------------------------------------------------
# Report & Ban
# -----------------------------------------------------------------------------
# Number of reports required before an agent is automatically banned.
REPORT_BAN_THRESHOLD=3
# Admin email for report notifications and system alerts.
ADMIN_EMAIL=admin@plaw.social
